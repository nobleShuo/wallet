spring:
  profiles:
    active: dev
  application:
    name: antube-wallet
  cloud:
    config:
      failFast: true
      discovery:
        enabled: true
        serviceId: data-cloud-config-server

eureka:
  client:
    serviceUrl:
      defaultZone:







